schema = "forge3d.anamnesis.irrelevant_inputs/1"

# Intentionally empty. Every currently declared input category can affect a
# pixel and therefore participates in both the key and reference output.
irrelevant_inputs = []

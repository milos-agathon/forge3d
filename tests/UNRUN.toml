# tests/UNRUN.toml
# CENSOR Task 13: files the default CI Python lane (scripts/ci_pytest_lane.py)
# must NOT collect, each with an honest, owner-attributed, expiring reason.
# A file belongs here ONLY if it fails or errors on a stock GitHub-hosted CI
# runner (no discrete GPU, no optional pip extras, only tracked files present)
# AND does not already self-skip. This list may only shrink.
#
# AUDIT METHOD (2026-07-10):
#   1. Full suite collected cleanly locally: 2936 tests, 0 collection errors.
#   2. Faithful CI simulation: a detached `git worktree` of HEAD, the built
#      _forge3d.pyd copied in, run with GITHUB_ACTIONS=true so the
#      hosted-CI terrain/GPU guards fire like a real runner. That single
#      `pytest tests/` invocation is authoritative because the CI lane runs the
#      whole tree at once (per-file/isolated runs disagree due to global GPU
#      and lighting state -- e.g. test_lighting_alignment passes in-suite).
#   3. Every paired example used by a collected test is committed; the root
#      conftest has no collection bypass for absent local files.
#   4. GPU/terrain golden tests self-skip via _terrain_runtime.terrain_rendering_available()
#      (has_gpu()/hosted-CI probe) and are NOT listed.
#   5. test_dask_stub self-skips on CI (real runners have no xarray) and is NOT listed.
#   6. Stale pure-CPU P1/P2 expectations were repaired against the current
#      supported feature surface; only hardware-bound files remain below.
#
# NOTE: the `[[entries]]` array-of-tables below IS the entries list; do not add a
# separate `entries = []` line (TOML forbids redefining an array-of-tables).

# --- requires a hardware GPU adapter with no graceful CPU skip -----------------
# SUTURA zero-placeholder MapScene render paths: MapScene.render raises
# MapSceneNativeUnavailable (not skip) when no terrain-safe adapter exists, so
# these fail on GPU-less runners. They are not in the GPU golden lane either.
[[entries]]
file = "tests/test_mapscene_buildings.py"
reason = "requires_hardware_adapter: MapScene native terrain/building render raises MapSceneNativeUnavailable with no CPU skip on GPU-less runners"
owner = "milos"
expires = "2026-10-10"

[[entries]]
file = "tests/test_mapscene_furniture.py"
reason = "requires_hardware_adapter: MapScene native terrain render raises MapSceneNativeUnavailable with no CPU skip on GPU-less runners"
owner = "milos"
expires = "2026-10-10"

[[entries]]
file = "tests/test_mapscene_render_png.py"
reason = "requires_hardware_adapter: MapScene native offscreen terrain render raises MapSceneNativeUnavailable with no CPU skip on GPU-less runners"
owner = "milos"
expires = "2026-10-10"

[[entries]]
file = "tests/test_mapscene_screen_space_settings.py"
reason = "requires_hardware_adapter: MapScene native terrain/screen-space render raises MapSceneNativeUnavailable with no CPU skip on GPU-less runners"
owner = "milos"
expires = "2026-10-10"

[[entries]]
file = "tests/test_mapscene_sutura_integrity.py"
reason = "requires_hardware_adapter: save/load re-render SSIM needs native terrain adapter; raises MapSceneNativeUnavailable with no CPU skip on GPU-less runners"
owner = "milos"
expires = "2026-10-10"

# --- CI-only decode divergence under investigation ----------------------------
[[entries]]
file = "tests/test_cog_streaming.py"
reason = "ci_only_lzw_divergence: tile reads fail on hosted runners with LZW 'code out of range' while the identical LFS object decodes cleanly across every tile locally (verified 2026-07-11); needs on-runner debugging of the fetched bytes/decoder"
owner = "milos"
expires = "2026-08-15"

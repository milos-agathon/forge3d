# TERMINUS torture atlas

Committed descriptor corpus for antimeridian, pole, raster, DEM, CRS, label, and scene edge cases.
Each JSON file is data-only and is executed by `tests/test_torture_atlas.py` through `tests/_torture.py`.
